[package]
name = "day03"
version = "0.1.0"
edition = "2021"

[dependencies]
once_cell = "1.20.2"
regex = { version = "1.11.1", features = ["pattern"] }
santas_little_helpers = { path = "../santas_little_helpers" }
